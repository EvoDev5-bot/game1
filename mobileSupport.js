export class DeviceOrientation {
  constructor(game) {
    // this.tilt = null;
    // document.addEventListener(
    //   "deviceorientation",
    //   (e) => {
    //     alert(e.alpha);
    //   },
    //   true
    // );
  }
}
